<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>Login Page</title>
  <link rel="stylesheet" href="/styles/auth.css" />
    <script src="https://cdn.tailwindcss.com"></script>
</head>

<body class="bg-gray-100 min-h-screen flex items-center justify-center">

  <div class="auth-container w-full max-w-md">

    <h1 class="admin-title text-2xl font-bold text-center mb-4">ADMIN LOGIN</h1>

    <div class="pawsome-container flex justify-center mb-4">
      <img src="/img/Remove background project.png" alt="pawsome" class="pawsome-icon ">
    </div>

    <form id="loginform" action="/admin/login" method="POST" class="space-y-4 w-full">

      <% if(typeof error !=='undefined' ) { %>
        <div class="text-red-500 text-sm text-center">
          <%=error%>
        </div>
        <% } %>

          <div>
            <input type="email" id="email" name="email" placeholder="Email"
              class="w-full px-4 py-2 border border-gray-300 rounded focus:outline-none focus:ring-2 focus:ring-yellow-500" />
            <div id="error1" class="text-red-500 text-sm mt-1"></div>
          </div>

          <div>
            <input type="password" id="password" name="password" placeholder="Password"
              class="w-full px-4 py-2 border border-gray-300 rounded focus:outline-none focus:ring-2 focus:ring-yellow-500" />
            <div id="error2" class="text-red-500 text-sm mt-1"></div>
          </div>

          <button type="submit"
            class="w-full bg-yellow-500 text-white py-3 rounded font-semibold hover:bg-yellow-600 transition">
            LOGIN
          </button>
    </form>
  </div>

</body>


</html>